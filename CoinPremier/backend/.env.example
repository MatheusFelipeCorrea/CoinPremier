# Server
PORT=3333
NODE_ENV=development

# Database (Neon ou local)
DATABASE_URL="postgresql://usuario:senha@localhost:5432/coinpremier?schema=public"

# JWT
JWT_SECRET="troque-esta-chave-super-secreta-em-producao"
JWT_EXPIRES_IN="7d"

# Email (SMTP)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="seu-email@gmail.com"
SMTP_PASS="sua-senha-de-app"
SMTP_FROM="CoinPremier <nao-responda@coinpremier.com>"

# Upload
UPLOAD_MAX_SIZE_MB=5
UPLOAD_PATH="./uploads"

# Frontend
FRONTEND_URL="http://localhost:5173"